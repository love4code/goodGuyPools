      </div>
    </main>
    <footer class="border-top py-4 mt-5">
      <div class="container small d-flex flex-column flex-md-row justify-content-between align-items-center">
        <div>
          <strong><%= (siteSettings && siteSettings.companyName) || 'Goodfella Pools' %></strong>
          <div class="text-muted"><%= (siteSettings && siteSettings.address) || '' %></div>
          <div class="text-muted">
            <a href="tel:<%= (siteSettings && siteSettings.phone) || '' %>"><%= (siteSettings && siteSettings.phone) || '' %></a>
            ·
            <a href="mailto:<%= (siteSettings && siteSettings.email) || '' %>"><%= (siteSettings && siteSettings.email) || '' %></a>
          </div>
        </div>
        <div class="mt-3 mt-md-0">
          <% if (siteSettings && siteSettings.social) { %>
            <% if (siteSettings.social.facebookUrl) { %><a class="me-3" href="<%= siteSettings.social.facebookUrl %>" target="_blank">Facebook</a><% } %>
            <% if (siteSettings.social.instagramUrl) { %><a class="me-3" href="<%= siteSettings.social.instagramUrl %>" target="_blank">Instagram</a><% } %>
            <% if (siteSettings.social.youtubeUrl) { %><a class="me-3" href="<%= siteSettings.social.youtubeUrl %>" target="_blank">YouTube</a><% } %>
            <% if (siteSettings.social.tiktokUrl) { %><a href="<%= siteSettings.social.tiktokUrl %>" target="_blank">TikTok</a><% } %>
          <% } %>
        </div>
        <div class="text-muted mt-3 mt-md-0">&copy; <%= new Date().getFullYear() %> Goodfella Pools</div>
      </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>


